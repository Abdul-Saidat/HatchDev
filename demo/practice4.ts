class ListNode<T> {
    data: T
    next: ListNode<T> | null = null

    constructor(data: T) {
        this.data = data
    }
}

class SinglyLinkedList<T>  {
    head: ListNode<T> | null = null

    constructor() {
        
    }

    // add a new node to the end of the list
    // add a new node inside the list at a specified position
    // change head
    //get the length of the list
    // remove the tail and anyone in a specific position

}